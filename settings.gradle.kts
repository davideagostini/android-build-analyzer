pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}

rootProject.name = "android-build-analyzer"

dependencyResolutionManagement {
    repositories {
        google()
        mavenCentral()
    }
}
